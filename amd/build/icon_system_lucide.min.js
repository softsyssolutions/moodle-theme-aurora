/**
 * Lucide icon system for theme_aurora (hand-written AMD build of amd/src/icon_system_lucide.js).
 *
 * @module     theme_aurora/icon_system_lucide
 * @copyright  2026 Industria Elearning
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("theme_aurora/icon_system_lucide", ["exports", "core/icon_system", "core/mustache", "core/url", "theme_aurora/lucide_map"], function (_exports, _icon_system, Mustache, CoreUrl, _lucide_map) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", { value: true });
  _exports.default = void 0;
  _icon_system = _interopRequireDefault(_icon_system);
  _lucide_map = _interopRequireDefault(_lucide_map);

  function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

  class IconSystemLucide extends _icon_system.default {
    /**
     * Render an icon.
     *
     * @param {string} key
     * @param {string} component
     * @param {string} title
     * @param {string} template
     * @return {string}
     */
    renderIcon(key, component, title, template) {
      const mappedComponent = (typeof component === 'undefined' || component === 'moodle' || component === '')
        ? 'core'
        : component;
      const mapKey = `${mappedComponent}:${key}`;
      const alt = (typeof title === 'undefined') ? '' : title;

      if (Object.prototype.hasOwnProperty.call(_lucide_map.default, mapKey)) {
        const [svg, classes] = _lucide_map.default[mapKey];
        const context = {
          unmappedIcon: false,
          svg: svg,
          title: alt,
          alt: alt,
          extraclasses: classes
        };
        if (alt === '') {
          context['aria-hidden'] = 'true';
        }
        return Mustache.render(template, context).trim();
      }

      // Unmapped: standard pix <img> fallback.
      const url = CoreUrl.imageUrl(key, component);
      const attributes = [
        { name: 'src', value: url },
        { name: 'alt', value: alt },
        { name: 'title', value: alt }
      ];
      if (alt === '') {
        attributes.push({ name: 'aria-hidden', value: 'true' });
      }
      return Mustache.render(template, {
        unmappedIcon: true,
        attributes: attributes,
        extraclasses: ''
      }).trim();
    }

    /**
     * Get the name of the template to pre-cache for this icon system.
     *
     * @return {string}
     */
    getTemplateName() {
      return 'theme_aurora/pix_icon_lucide';
    }
  }

  _exports.default = IconSystemLucide;
  return _exports.default;
});
